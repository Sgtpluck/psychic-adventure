<h1>All my products!</h1>
<div class='row'>
  <div class='col-lg-2'>
    <h4>All Products</h4>
     <ul class='list-unstyled'>
      {{# each }}
      <li> {{# link-to "products" }} 
        {{ name }}
        {{/ link-to }}
        ${{ format-price price }}
      </li>
      {{/ each }}
    </ul>
  </div>
  <div class='col-lg-10'>
   Product View Goes Here
  </div>
</div>